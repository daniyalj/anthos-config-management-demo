apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: argocd-virtual-service
spec:
  hosts:
  - "argocd.anthos.daniyalj.team.arctiq.ca"
  gateways:
  - istio-system/ingressgateway
  http:
  - route:
    - destination:
        host: argocd-server
        port:
          number: 80
